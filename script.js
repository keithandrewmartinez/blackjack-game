// First code will focus on creating a deck programmatically (as opposed to hardcoding each card)
// This will be done per the advice of MrGcoding's Youtube tutorial on the subject


// array of 'suits' that has one letter for each of the following ('hearts', 'clubs', 'diamonds', 'spades'];

let suits = ['H', 'C', 'D', 'S'];

// an array of 'ranks' for each card "value"
let ranks = ['2', '3', '4', '5', '6', '7', '8', '9', 'J', 'Q', 'K', 'A'];


// create a for loop to produce each card (combination of 1 rank and 1 suit);

for(i = 0; i < 4; i++) {
    
    console.log(suits[i])
 for(j = 0; j < 13; j++) 
    
    console.log(suits[i])
}